# Generate both secrets thus:
# python -c "import secrets, pathlib; p = pathlib.Path('.env'); p.write_text(''.join(line.replace('\"SECRET\"', '\"%s\"' % secrets.token_urlsafe(48)) for line in p.read_text().splitlines(keepends=True)))"
SECRET_KEY = "SECRET"

# Pick one
DEPLOY_TYPE = "dev"
DEPLOY_TYPE = "test"
DEPLOY_TYPE = "prod"

# Shared secret for the Google Sheets webhook (App Script -> Django).
SHEETS_WEBHOOK_API_KEY = "SECRET"

# Fill in
# EMAIL_HOST = 'mail.smtp25.com'
# EMAIL_PORT = 2525
# EMAIL_HOST_USER = 'smtpuser'
# EMAIL_HOST_PASSWORD = 'smtppassword'
